language: node_js

node_js:
  - "0.10"
  - "4"

addons:
  apt:
    sources: [ 'ubuntu-toolchain-r-test' ]
    packages: [ 'libstdc++-5-dev' ]

sudo: false

install:
- npm install

script:
- npm test

